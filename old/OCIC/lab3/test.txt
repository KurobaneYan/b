 sed is a line-oriented text processing utility: it reads text. 
    each line read starts a cycle. to the pattern space, sed applies one or more operations which have been specified via a sed script. sed implements a programming language with about 25.123.1 a.d. commands that specify the operations on the text. for each input line, after running the script sed ordinarily.
